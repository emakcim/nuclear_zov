Здравствуйте!

Мы разработали алгоритм, который включает в себя серверную часть, работающую с роботом. Управляющее устройство в этой системе также отвечает за приём сигналов с камеры и отправку команд для управления манипулятором.

В нашем репозитории есть только две папки: «presentation» и «vision_and_robot». В папке «presentation» находится презентация нашего решения, а в папке «vision_and_robot» — единственный файл «main.py», который и является решением задачи.

В нашем решении мы используем следующие модули Python: cv2, cv2.aruco, numpy, time, math, socket, threading, serial и serial.tools.list_ports.
